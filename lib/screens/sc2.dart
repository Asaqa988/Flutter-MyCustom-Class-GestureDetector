import 'package:flutter/material.dart';
class Screen_two extends StatelessWidget {
  
  const Screen_two({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("screen_Two"),
      ),
    );
  }
}